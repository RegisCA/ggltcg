# GGLTCG Environment Variables
# Copy this file to .env and fill in your API key
# DO NOT commit .env to git!

# ===== AI Player Configuration =====

# Google Gemini API Key (FREE)
# Get a free key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Optional: Override default Gemini model
# Default: gemini-2.0-flash-lite (30 RPM, best free tier but can have capacity issues)
# If you get 429 capacity errors, try these alternatives:
#   gemini-2.5-flash (newest, more stable, good capacity)
#   gemini-flash-latest (automatically uses latest stable flash model)
#   gemini-2.0-flash (stable, proven)
# GEMINI_MODEL=gemini-2.5-flash

# Optional: Fallback model when primary model hits capacity (429 errors)
# Default: gemini-2.5-flash-lite (15 RPM, better capacity availability)
# This model is automatically used if the primary model returns 429 errors after retries
# GEMINI_FALLBACK_MODEL=gemini-2.5-flash-lite

# Optional: Use Claude instead (requires paid Anthropic API credits)
# ANTHROPIC_API_KEY=your-anthropic-key-here
# AI_PROVIDER=anthropic
